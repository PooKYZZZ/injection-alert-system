# WAF-ML SOC Dashboard - ML Training dependencies
# Install runtime deps first: pip install -r requirements.txt
# Then: pip install -r requirements.train.txt
# CPU training is supported. Install an accelerator-specific PyTorch build only
# when the official PyTorch selector recommends one for the target machine.
# Training - ML fine-tuning and evaluation
# Install AFTER requirements.txt
# Supported Python version: 3.14+ (see pyproject.toml)

accelerate==1.13.0
ipykernel==7.2.0
ipywidgets==8.1.8
jupyter_client==8.8.0
jupyter_core==5.9.1
matplotlib==3.10.8
pandas==3.0.1
pyarrow==23.0.1
scikit-learn==1.8.0
scipy==1.17.1
seaborn==0.13.2
tensorboard==2.20.0
tqdm==4.67.1

# Note: `notebook`, `datasets`, and the Hugging Face `evaluate` package are not
# required by the checked-in training workflow.
# torch, transformers, and numpy are provided by requirements.txt so the runtime
# and training environments use one compatible version of each package.
